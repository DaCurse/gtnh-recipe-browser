diff --git a/BrowserCatalogPolicy.cs b/BrowserCatalogPolicy.cs
new file mode 100644
index 0000000..70659ad
--- /dev/null
+++ b/BrowserCatalogPolicy.cs
@@ -0,0 +1,10 @@
+namespace Source
+{
+    // ShadowTheAge's blacklist is appropriate for a production calculator, but this
+    // project retains every item reached by recipes, containers, or crafter metadata.
+    // PackConverter's existing touched-item pass still excludes unrelated NEI state.
+    public static class BrowserCatalogPolicy
+    {
+        public const bool RetainRecipeConnectedItems = true;
+    }
+}
diff --git a/ItemBanlist.cs b/ItemBanlist.cs
index 51cf4af..ab30e38 100644
--- a/ItemBanlist.cs
+++ b/ItemBanlist.cs
@@ -137,6 +137,9 @@
         
         public static bool IsItemBanned(string mod, string internalName, string localizedName, string nbt)
         {
+            if (BrowserCatalogPolicy.RetainRecipeConnectedItems)
+                return false;
+
             if (BanList.TryGetValue((mod, internalName), out var processor))
                 return processor == null || processor(localizedName, nbt);
             return false;
diff --git a/AtlasBuilder.cs b/AtlasBuilder.cs
index b01d6e4..2c4cb9b 100644
--- a/AtlasBuilder.cs
+++ b/AtlasBuilder.cs
@@ -24,6 +24,13 @@ namespace Source
         {
             var entry = archive.GetEntry(path);
-            if (entry == null)
+            var fileName = Path.GetFileNameWithoutExtension(path);
+            var firstSeparator = fileName.IndexOf('~');
+            var lastSeparator = fileName.LastIndexOf('~');
+            var hasVariantIdentity = firstSeparator >= 0 && firstSeparator != lastSeparator;
+
+            // Generic goods may intentionally resolve to their first rendered sibling.
+            // NBT-specific goods must match exactly or the wrong variant would be shown.
+            if (entry == null && !hasVariantIdentity)
             {
                 path = path.Substring(0, path.Length - 4);
                 foreach (var iEntry in archive.Entries)
@@ -40,6 +47,7 @@ namespace Source
             if (entry == null)
             {
-                Console.WriteLine("Unable to find archive entry for " + path);
-                return null;
+                throw new InvalidDataException(
+                    "Image archive has no render for " + path +
+                    ". Mutable item renderers must preserve their pre-render image path.");
             }
             using var stream = entry.Open();
@@ -81 +89,3 @@ namespace Source
-                    var positionY = ((i & IconAtlas.YMask) >> IconAtlas.DimensionBits) * IconAtlas.ImageSize;
+                    // The browser catalog can exceed the original 65,536-sprite page.
+                    // The allocated atlas is already tall enough, so do not wrap later rows.
+                    var positionY = (i >> IconAtlas.DimensionBits) * IconAtlas.ImageSize;
diff --git a/PackPreProcessor.cs b/PackPreProcessor.cs
index cb8633b..bb9ec58 100644
--- a/PackPreProcessor.cs
+++ b/PackPreProcessor.cs
@@ -140,12 +140,20 @@ namespace Source
                 var parts = item.tooltip.Split('\n');
                 builder.Clear();
-                for (var i = 1; i < parts.Length-1; i++)
+                // ItemStack#getTooltip supplies the display name first, but it does not
+                // guarantee a redundant footer. The final line frequently contains real
+                // effects, durability, material stats, or other gameplay information.
+                for (var i = 1; i < parts.Length; i++)
                 {
                     var part = parts[i];
+                    var cleanedPart = Regex.Replace(part, "§.", "").Trim();
+                    if (cleanedPart.Equals("shift", StringComparison.OrdinalIgnoreCase) ||
+                        cleanedPart.Equals("ctrl", StringComparison.OrdinalIgnoreCase) ||
+                        cleanedPart.Equals("control", StringComparison.OrdinalIgnoreCase))
+                        continue;
                     if ((part.Contains("press", StringComparison.OrdinalIgnoreCase) || part.Contains("hold", StringComparison.OrdinalIgnoreCase)) &&
                         (part.Contains("ctrl", StringComparison.OrdinalIgnoreCase) || part.Contains("shift", StringComparison.OrdinalIgnoreCase) || part.Contains("control", StringComparison.OrdinalIgnoreCase)))
                         continue;
                     if (i == 1 && part == "")
                         continue;
 
                     builder.Append(part).Append('\n');
