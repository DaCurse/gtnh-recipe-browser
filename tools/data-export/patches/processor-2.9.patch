diff --git a/PackConverter.cs b/PackConverter.cs
index b709151..052de58 100644
--- a/PackConverter.cs
+++ b/PackConverter.cs
@@ -347,0 +348,6 @@ namespace Source
+            foreach (var aspectModel in generator.GetTableContents(aspect))
+            {
+                if (items.TryGetValue(aspectModel.IconId, out var aspectIcon) && aspectIcon != null)
+                    aspectIcon.touched = true;
+            }
+
diff --git a/OldRecipesGenerator.cs b/OldRecipesGenerator.cs
index a37f878..d465c50 100644
--- a/OldRecipesGenerator.cs
+++ b/OldRecipesGenerator.cs
@@ -64,0 +65 @@ public class OldRecipesGenerator
+        var indexedRecipeCount = 0;
@@ -81,0 +83,5 @@ public class OldRecipesGenerator
+            if (++indexedRecipeCount % 10_000 == 0)
+            {
+                GC.Collect();
+                Console.WriteLine($"Indexed {indexedRecipeCount} current recipes; managed heap: {GC.GetTotalMemory(false)} bytes");
+            }
@@ -86,0 +93 @@ public class OldRecipesGenerator
+        var matchedRecipeCount = 0;
@@ -89,0 +97,5 @@ public class OldRecipesGenerator
+            if (++matchedRecipeCount % 10_000 == 0)
+            {
+                GC.Collect();
+                Console.WriteLine($"Matched {matchedRecipeCount} historical recipes; managed heap: {GC.GetTotalMemory(false)} bytes");
+            }
diff --git a/PackPreProcessor.cs b/PackPreProcessor.cs
index 68f9fc0..2d40c65 100644
--- a/PackPreProcessor.cs
+++ b/PackPreProcessor.cs
@@ -111 +111,8 @@ namespace Source
-            var aspects = repository.items.Where(x => x.mod == "thaumcraftneiplugin" && x.name.StartsWith("Aspect: ")).ToDictionary(x => x.name.Substring("Aspect: ".Length));
+            var aspects = repository.items
+                .Where(x =>
+                    (x.mod == "thaumcraftneiplugin" && x.name.StartsWith("Aspect: "))
+                    || (x.mod == "aspectrecipeindex" && x.internalName == "aspect"))
+                .ToDictionary(
+                    x => x.mod == "thaumcraftneiplugin"
+                        ? x.name.Substring("Aspect: ".Length)
+                        : x.name);
diff --git a/PackGenerator.cs b/PackGenerator.cs
index 503b7eb..f4438ec 100644
--- a/PackGenerator.cs
+++ b/PackGenerator.cs
@@ -14,0 +15,5 @@ namespace Source
+            dbParser = null;
+            GC.Collect();
+            GC.WaitForPendingFinalizers();
+            GC.Collect();
+
@@ -19,0 +25,5 @@ namespace Source
+            GC.Collect();
+            GC.WaitForPendingFinalizers();
+            GC.Collect();
+            Console.WriteLine($"Managed heap before recipe remaps: {GC.GetTotalMemory(false)} bytes");
+
